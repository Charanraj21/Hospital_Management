<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>K HOSPITAL</title>
    <title>Registration</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/index.css">
</head>

<body>
    <h1>K HOSPITAL</h1>
    <%- include('includes/nav.ejs')%>
    <h2>Doctor Login</h2>
    <div class="container border rounded col-md-6" style="background: rgb(240, 248, 255); border-color: black;">
        <% if (error.length > 0) { %>
        <div class="alert alert-danger text-center mt-2" role="alert">
            <%= error %>
        </div>
        <% } %>
        <form class="p-3" method="POST">

            <div class="form-group">
                <label for="inputEmail4">Email</label>
                <input type="email"
                    class="form-control <%= validationErrors.find(e => e.param === 'email') ? 'is-invalid' : '' %>"
                    id="inputEmail4" name="email" placeholder="Email" value="<%= data.email %>" required>
            </div>
            <div class="form-group">
                <label for="inputPassword4">Password</label>
                <input type="password"
                    class="form-control <%= validationErrors.find(e => e.param === 'password' || 'email') ? 'is-invalid' : '' %>"
                    id="inputPassword4" name="password" placeholder="Password" value="<%= data.password %>" required>
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-primary">Login</button>
            </div>
            <div class="text-center mt-2">
                <br>
                <small>
                    Patient? Login <a href="/login/patient">Here</a>
                </small>
                <br>
                <small>
                    Admin? Login <a href="/login/admin">Here</a>
                </small>
            </div>
        </form>
    </div>
</body>
<%-include('includes/end.ejs') %>

</html>