<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>K HOSPITAL</title>
    <title>Registration</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/index.css">

</head>

<body>
    <h1>K HOSPITAL</h1>
    <%- include('includes/nav.ejs')%>
    <h2>My Profile</h2>
    <div class="container border rounded" style="background: rgb(240, 248, 255); border-color: black;">
        <% if (error.length > 0) { %>
        <div class="alert alert-danger text-center mt-2" role="alert">
            <%= error %>
        </div>
        <% } %>
        <form class="p-3" method="POST">
            <div class="form-group">
                <label for="Name">Name</label>
                <input type="text"
                    class="form-control <%= validationErrors.find(e => e.param === 'pname') ? 'is-invalid' : '' %>"
                    id="Name" name="pname" placeholder="Name" value="<%= data.pname %>" required>
            </div>

            <div class="form-group">
                <label for="inputEmail4">Email</label>
                <input type="email"
                    class="form-control <%= validationErrors.find(e => e.param === 'email') ? 'is-invalid' : '' %>"
                    id="inputEmail4" name="email" placeholder="Email" value="<%= data.email %>" required>
            </div>

            <div class="form-group">
                <label for="inputAddress">Address</label>
                <input type="text"
                    class="form-control <%= validationErrors.find(e => e.param === 'address') ? 'is-invalid' : '' %>"
                    id="inputAddress" name="address" placeholder="Address" value="<%= data.address %>" required>
            </div>
            <div class="form-group">
                <label for="pno">Phone No</label>
                <input type="tel"
                    class="form-control <%= validationErrors.find(e => e.param === 'pno') ? 'is-invalid' : '' %>"
                    id="pno" name='pno' placeholder="Phone Number" value="<%= data.pno %>" required>
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-primary">Update</button>
            </div>
            <div class="text-center">
                <small class="text-center">
                    You change Password <a href="/patient/changepass">Here</a>
                </small>
            </div>
        </form>
    </div>
</body>
<%-include('includes/end.ejs') %>

</html>